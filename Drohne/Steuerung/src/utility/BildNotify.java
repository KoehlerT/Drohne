package utility;

public interface BildNotify {
	/*Interface für Klassen, die bei neuen Bildern informiert werden wollen.
	 * Sie brauchen nur eine Methode, die aufgerufen wird, wenn ein neues Bild entstanden ist
	 * */
	public void neuesBild(); 
}
